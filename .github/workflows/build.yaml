name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 6
      - run: npm install
      - run: npm build
